# Evaluation environment specific dependencies
# These are isolated from the main project to avoid version conflicts

# Core project dependencies (pinned for stability and compatibility)
# vLLM requires specific torch versions; 2.4.0 is widely compatible with vLLM 0.6.x
numpy==1.26.4
protobuf==5.27.2
torch==2.4.0
torchvision==0.19.0
transformers==4.46.3

# vLLM (pinned to a stable, real version)
vllm==0.6.3.post1

# Evaluation Harness Core
lm-eval==0.4.11
evaluate==0.4.0
rouge-score==0.1.2
datasets
pydantic>=2.8.0
accelerate==1.1.1

# Specialized requirements
langdetect
pybind11
numexpr
immutabledict
sympy
math_verify
antlr4-python3-runtime==4.11
